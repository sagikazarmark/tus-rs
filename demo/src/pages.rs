//! One route component per gallery page. Each wraps a live example from
//! [`crate::examples`] with prose and the example's own source.

pub mod controls;
pub mod home;
pub mod minimal;
pub mod options;
pub mod queue;
pub mod resume;
