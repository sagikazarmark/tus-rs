type Tus {
  let rust: Rust!

  new(workspace: Workspace!) {
    self.rust = Dagger.rust(workspace)

    self
  }

  """
  The tus CLI binary.
  """
  cli: File! {
    self
      .rust
      .build(bin: ["tus"])
      .file("tus")
  }
}
